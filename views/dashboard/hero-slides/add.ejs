<div class="dashboard-content">
    <form action="/hero-slides/dashboard/add" method="POST" enctype="multipart/form-data">
        <div class="card shadow">
            <div class="card-body">
                <div class="row">
                    <div class="form-group col-md-6">
                        <label>Main Title</label>
                        <input type="text" name="title" class="form-control" required placeholder="Adventure awaits">
                    </div>
                    <div class="form-group col-md-6">
                        <label>Highlighted Title (End)</label>
                        <input type="text" name="titleEnd" class="form-control" placeholder="In Africa">
                    </div>
                </div>
                <div class="form-group">
                    <label>Caption/Subtitle</label>
                    <textarea name="caption" class="form-control" rows="2"></textarea>
                </div>
                <div class="form-group">
                    <label>Display Order (0 = First)</label>
                    <input type="number" name="order" class="form-control" value="0">
                </div>
                <div class="form-group">
                    <label>Background Image</label>
                    <input type="file" name="photo" class="form-control-file" required id="slidePhoto" accept="image/*">
                    <div id="preview" class="mt-3"></div>
                </div>
            </div>
            <div class="card-footer">
                <button type="submit" class="btn btn-success">Create Slide</button>
            </div>
        </div>
    </form>
</div>
<script>
    document.getElementById('slidePhoto').addEventListener('change', function(e) {
        if(e.target.files[0]) {
            const reader = new FileReader();
            reader.onload = (ex) => { document.getElementById('preview').innerHTML = `<img src="${ex.target.result}" style="width:100%; max-height:200px; object-fit:cover; border-radius:8px;">`; };
            reader.readAsDataURL(e.target.files[0]);
        }
    });
</script>